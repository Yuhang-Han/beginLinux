20260729
本目录为第三章文件操作的实践

copy 系列程序分别使用系统调用和库函数配合逐个字符读写及整块读写测试了效率.
测试所用文件file.in 是重复复制了几篇论文得到的, 大小约28MB.

copy_system.c
使用系统调用read 和write 逐字符读写, 用时21.29 s

copy_block.c
仍使用系统调用, 但一次读写1KB 数据, 用时0.02 s

copy_stdio.c
使用标准库stdio 中fread 和fwrite 逐字符读写, 用时 0.19 s

copy_stdioblock.c
同样使用标准库, 但一次读写1KB 数据, 用时 0.03 s


总结

对比发现, 影响速度的关键因素是数据块的大小

由于反复系统调用带来的切换很耗费时间, 所以copy_system 显著慢
而标准库通过缓冲节省了一部分这种操作, 
在逐字符读写下, copy_stdio 会比 copy_system 快许多

而在同样为1KB 的数据块下stdio 并不会更快
