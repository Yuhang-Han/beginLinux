20260730

第四章第一节介绍如何像bash 命令一样读取参数
c语言中的参数是从main(int argc, char *argv[]) 读到的
argc 是参数个数, 包含程序名
argv[] 是一个字符串数组, 存储各个参数

getopt.c
手动地从参数中分拆出选项和参数

argopt.c
使用unistd 中的函数getopt() 自动而规范地处理参数

longopt.c
再套一个getopt_long() 以兼容长选项.


20260731

第二节讲环境变量.
个人觉得环境变量就像是全局变量, 用起来很方便, 但也危险.
linux 里的环境变量好像一般用来设置路径和一些全局设置选项.

environ.c
使用getenv(), putenv() 分别读取和设置环境变量.
注意后者仅改变程序运行环境中的环境变量, 不会对父进程作用.

showenv.c
读取外部变量environ 来打印所有环境变量.


第三节讲时间, 
可以用time() 获取UNIX 纪元起的秒数(GMT), 
然后用localtime() 转成当地时间(UTC),
最后strftime() 很方便地以特定格式排版时间和日期.
